import SwiftUI

// Simple test version to check if basic structure works
struct BankConnectionTestView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Bank Connection Test")
                Text("This is a simple test version")
            }
            .navigationTitle("Bank Connections")
        }
    }
}